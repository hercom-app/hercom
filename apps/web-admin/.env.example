# URL del deployment de Convex.
# Local: la imprime `pnpm --filter @proyecto/backend dev`
# Producción (Vercel admin.hercom.pe): la de `npx convex deploy`
VITE_CONVEX_URL=https://your-deployment.convex.cloud
